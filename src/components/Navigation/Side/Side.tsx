import * as React from 'react';
import { DefaultButton } from '../../Common/Button';
import './Side.scss';

export const Side = () => (
  <div className="navigation__side">
    <DefaultButton />
  </div>
);
